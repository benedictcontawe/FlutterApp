import 'package:flutter/material.dart';

class ContainerFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
              child: Center(child:Text("Page 3")),
              color: Colors.white,
    );
  }
}